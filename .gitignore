# compiled ruby script archive (.exa) / binary (.exe)
*.ex[ae]

# compiled resource output
res*.o
# the following files will be generated by "msg.mcp" by preprocessor (CPP) and message compiler (MC)
msg.[mrh]c
*.bin

# compiled static library
/compile/*.a
/compile/test*

# packages downloaded from github repos
/vendor/ruby
/vendor/zlib
/vendor/exerb-mingw

# win32/api compiled library and other junks
/win32

# compilation temp outputs
/tmp

# other test-related files
manifest.txt
/BGM
/tswLauncher/TSW1.2r*
